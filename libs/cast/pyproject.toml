[project]
name = "cast"
version = "0.1.0"
description = "Cast knowledge base helpers"
requires-python = ">=3.13"
dependencies = ["litellm>=1.44.4"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cast"]
