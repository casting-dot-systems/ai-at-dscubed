[project]
name = "org_types"
version = "0.1.0"
description = "Type definitions for AI at D3"
requires-python = ">=3.13"
dependencies = ["pydantic>=2.11.3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["org_types"]
